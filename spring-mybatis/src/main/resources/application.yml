spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smtest?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/static/


mybatis:
  mapper-locations: classpath:/mapper/*.xml
